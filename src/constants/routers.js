export const PATHS = {
  ROOT: '/',
  LOGIN: '/auth/login',
  REGISTER: '/auth/register',
  AUTHENTICATION: '/auth/',
  PROFILE: '/profile',
  CHAT: '/chat',
  HOME: '/home',
};

const ROUTERS = { PATHS };

export default ROUTERS;
